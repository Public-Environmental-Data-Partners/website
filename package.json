{
    "name": "PEDP website",
    "private": true,
    "version": "1.0.0",
    "description": "PEDP website",
    "scripts": {
      "dev": "pnpm run --parallel dev",
      "build": "pnpm run --parallel build"
    },
    "engines": {
      "node": ">=24.0.0 <25",
      "pnpm": ">=10.0.0"
    }
  }