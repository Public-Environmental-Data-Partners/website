# Copy to .env and fill in values (Sanity CLI loads .env from this directory).

SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Origin of the Next.js site (for Brand SVG picker previews). Default: http://localhost:3000
# SANITY_STUDIO_WEB_ORIGIN=http://localhost:3000
